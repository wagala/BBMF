% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_beta_g.R
\name{update_beta_g}
\alias{update_beta_g}
\title{Update the Feature-Specific Probabilities Beta}
\usage{
update_beta_g(H, psi_g, b1, b2, c1, c2)
}
\arguments{
\item{H}{A binary matrix of dimension \eqn{R \times G} containing
the current factor-specific feature patterns.}

\item{psi_g}{A binary vector of length \eqn{G} containing the
feature-specific sparsity indicators \eqn{\psi_g}.}

\item{b1}{First shape parameter of the Beta prior used when
\eqn{\psi_g = 1}.}

\item{b2}{Second shape parameter of the Beta prior used when
\eqn{\psi_g = 1}.}

\item{c1}{First shape parameter of the Beta prior used when
\eqn{\psi_g = 0}.}

\item{c2}{Second shape parameter of the Beta prior used when
\eqn{\psi_g = 0}.}
}
\value{
A numeric vector of length \eqn{G} containing the updated
feature-specific probabilities \eqn{\beta_g}.
}
\description{
Samples the feature-specific probabilities \eqn{\beta_g} from their
full conditional Beta distributions given the current binary matrix
\code{H} and the sparsity indicators \eqn{\psi_g}.
}
\details{
For each feature \eqn{g}, the function computes the number of active
entries in column \eqn{g} of \code{H},

\deqn{
H_g = \sum_{r=1}^{R} H_{rg}.
}

If \eqn{\psi_g = 1}, the full conditional distribution is

\deqn{
\beta_g \mid - \sim
\mathrm{Beta}(H_g + b_1,\; R - H_g + b_2).
}

If \eqn{\psi_g = 0}, the full conditional distribution is

\deqn{
\beta_g \mid - \sim
\mathrm{Beta}(H_g + c_1,\; R - H_g + c_2).
}
}
