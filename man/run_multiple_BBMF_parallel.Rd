% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_multiple_BBMF_parallel.R
\name{run_multiple_BBMF_parallel}
\alias{run_multiple_BBMF_parallel}
\title{Run Multiple BBMF Chains in Parallel}
\usage{
run_multiple_BBMF_parallel(
  X,
  W_o,
  H_o,
  n_iter,
  burn_in,
  n_chains,
  a1,
  a2,
  b1,
  b2
)
}
\arguments{
\item{X}{An observed binary matrix of dimension \eqn{K \times G}.}

\item{W_o}{Initial binary matrix of dimension \eqn{K \times R}
containing the factor activations.}

\item{H_o}{Initial binary matrix of dimension \eqn{R \times G}
containing the factor-specific feature patterns.}

\item{n_iter}{Total number of Gibbs sampling iterations for each chain.}

\item{burn_in}{Number of initial iterations discarded as burn-in for
each chain.}

\item{n_chains}{Number of independent MCMC chains to run.}

\item{a1}{First shape parameter of the Beta prior for the
row-specific probabilities \code{alpha}.}

\item{a2}{Second shape parameter of the Beta prior for the
row-specific probabilities \code{alpha}.}

\item{b1}{First shape parameter of the Beta prior for the
column-specific probabilities \code{beta}.}

\item{b2}{Second shape parameter of the Beta prior for the
column-specific probabilities \code{beta}.}
}
\value{
A named list of length \code{n_chains}. Each element contains
the output from \code{run_BBMF()} for the corresponding MCMC chain.
The list elements are named \code{Chain_1}, \code{Chain_2}, and so on.
}
\description{
Runs multiple independent Bayesian Boolean Matrix Factorization
MCMC chains in parallel using \code{run_BBMF()}.
}
\details{
Each MCMC chain is executed in parallel using \code{pbmclapply()}.
Before each chain is started, a chain-specific random seed is set
using \code{1000 + chain_id}, giving each chain a different
random-number sequence while preserving reproducibility.

The number of processor cores is determined using \code{detectCores()}.
An ETA-style progress indicator is displayed while the chains are
being executed.

Each chain starts from the supplied initial matrices \code{W_o}
and \code{H_o}.
}
